schema_version: "1.0"
# Andes CPU campaign: run ONLY activsg70k and activsg82k.
# All other cases are disabled (they run on Frontier GPU).
defaults:
  enabled: false
  timeout_s: 7200
  tier: andes_only
cases:
  activsg70k:
    enabled: true
  activsg82k:
    enabled: true
