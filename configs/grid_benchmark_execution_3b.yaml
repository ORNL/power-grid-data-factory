schema_version: "1.0"

defaults:
  enabled: true
  timeout_s: 120
  tier: benchmark

cases:
  pglib_opf_case10000_goc:
    timeout_s: 300
    tier: benchmark_slow
  pglib_opf_case4661_sdet:
    timeout_s: 300
    tier: benchmark_slow
  pglib_opf_case6470_rte:
    timeout_s: 300
    tier: benchmark_slow
  pglib_opf_case13659_pegase:
    timeout_s: 300
    tier: benchmark_timeout
  epigrids_eastern_78484:
    timeout_s: 300
    tier: benchmark_largest